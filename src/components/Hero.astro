---
import peacetower from "../assets/peacetower.svg";
import HeroTypewriter from "./HeroTypewriter.astro";
---

<section>
  <div class="hero">
    <div class="hero__text">
      <h1>OTTAWA</h1>
      <HeroTypewriter />
    </div>
    <img
      id="peacetower"
      src={peacetower.src}
      alt="Image of the Peace Tower"
      fetchpriority="high"
    />
  </div>
</section>

<style>
  section {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    background-color: var(--gray);
    padding: 0 1rem;
  }

  h1 {
    font-size: 2.25rem;
  }

  .hero {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    width: min(1000px, 100%);
    min-height: 595px;
    overflow-x: hidden;
    transition: width 0.3s ease;
    @media screen and (max-width: 1000px) {
      width: 100%;
      grid-template-columns: 1fr;
      place-items: center;
      gap: 1.25rem;
      padding: 1rem 2rem 0;
      min-height: auto;
    }
  }

  .hero__text {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 24px;
    text-align: center;
  }

  #peacetower {
    display: block;
    width: 100%;
    max-width: 470px;
    margin-left: 2rem;
    @media screen and (max-width: 1000px) {
      max-width: min(470px, 92vw);
      margin-left: 4rem;
      margin-bottom: 0;
    }
  }
</style>
